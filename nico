# Créé par nicolasthevenet, le 11/01/2026 en Python 3.7

#PARTIE 1
import random

nb_arbres=[random.randint(0,1) for i in range(6)]

def croissance_arbres(tableau, années):
    for i in range(années):
        for valeur in range(len(tableau)):
            grandi_ou_non=random.uniform(0,1)
            if grandi_ou_non<=0.7:
                tableau[valeur]+=1
        affichage_arbres(tableau)
        print()

def affichage_arbres(tableau):
    for i in range(len(tableau)):
        print(tableau[i]*"I")


def croissance_arbres_soleil(tableau, années):
    print(tableau)
    for i in range(années):
        for valeur in range(len(tableau)):
            if tableau[valeur-1]>tableau[valeur]:
                grandi_ombre=random.uniform(0,1)
                if grandi_ombre>=0.7:
                    tableau[valeur]+=random.randint(1,3)
            else:
                tableau[valeur]+=2
        affichage_arbres(tableau)
        print()

#PARTIE 2

def croissance_partie_2(tableau, années):
    print(tableau)
    for i in range(années):
        for valeur in range(len(tableau)):
            if tableau[valeur-1] and tableau[valeur+1]>tableau[valeur]:
                tableau[valeur]+=0
            if tableau[valeur-1] and tableau[valeur+1]<tableau[valeur]:
                grandi_soleil=random.uniform(0,1)
                if grandi_soleil<=0.9:
                    tableau[valeur]+=random.randint(1,4)
                else:
                    tableau[valeur]+=random.randint(0,3)
            else:
                grandi_mi_ombre=random.uniform(0,1)
                if grandi_mi_ombre<=0.5:
                    tableau[valeur]+=random.randint(1,3)
        affichage_arbres(tableau)
        print()





##################################################################

import random
def croissance_partie_2(tableau, années):
    print(tableau)
    for i in range(années):

        for valeur in range(len(tableau)):

            if tableau[valeur-1] > tableau[valeur] and tableau[valeur+1] > tableau[valeur]:
                tableau[valeur] += 0


            if tableau[valeur-1] < tableau[valeur] and tableau[valeur+1] < tableau[valeur]:
                grandi_soleil = random.uniform(0,1)


                if grandi_soleil<=0.9:
                    tableau[valeur]+=random.randint(1,4)

                else:
                    tableau[valeur]+=random.randint(0,3)

            else:
                grandi_mi_ombre=random.uniform(0,1)

                if grandi_mi_ombre<=0.5:
                    tableau[valeur]+=random.randint(1,3)

        affichage_arbres(tableau)
        print()

        # ia pour check si tout fonctionnait et voir les erreurs,
        # on faisait if tableau[valeur-1] and tableau[valeur+1]<tableau[valeur]: grandi_soleil=random.uniform(0,1)
        # alors que ca ne marche pas ChatGPT

#############################################

# Créé par nicolasthevenet, le 11/01/2026 en Python 3.7

#PARTIE 1
import random

nb_arbres=[random.randint(0,1) for i in range(6)]

def croissance_arbres(tableau, années):
    for i in range(années):
        for valeur in range(len(tableau)):
            grandi_ou_non=random.uniform(0,1)
            if grandi_ou_non<=0.7:
                tableau[valeur]+=1
        affichage_arbres(tableau)
        print()

def affichage_arbres(tableau):
    for i in range(len(tableau)):
        print(tableau[i]*"I")


def croissance_arbres_soleil(tableau, années):
    print(tableau)
    for i in range(années):
        for valeur in range(len(tableau)):
            if tableau[valeur-1]>tableau[valeur]:
                grandi_ombre=random.uniform(0,1)
                if grandi_ombre>=0.7:
                    tableau[valeur]+=random.randint(1,3)
            else:
                tableau[valeur]+=2
        affichage_arbres(tableau)
        print()

#PARTIE 2

def croissance_partie_2(tableau, années):
    print(tableau)
    n=len(tableau-1)
    for i in range(années):
        for valeur in range(len(tableau)):
            if tableau[valeur]!=n:
                if tableau[valeur-1] > tableau[valeur] and tableau[valeur+1] > tableau[valeur]:
                    tableau[valeur] += 0
                if tableau[valeur-1] < tableau[valeur] and tableau[valeur+1] < tableau[valeur]:
                    grandi_soleil = random.uniform(0,1)
                    if grandi_soleil<=0.9:
                        tableau[valeur]+=random.randint(1,4)
                    else:
                        tableau[valeur]+=random.randint(0,3)
                else:
                    grandi_mi_ombre=random.uniform(0,1)
                    if grandi_mi_ombre<=0.5:
                        tableau[valeur]+=random.randint(1,3)
            else:
                if tableau[valeur-1]<tableau[valeur]:
                    grandi_mi_ombre=random.uniform(0,1)
                    if grandi_mi_ombre<=0.5:
                        tableau[valeur]+=random.randint(1,3)
                else:
                    if tableau[valeur-1]>tableau[valeur]:
                        grandi_mi_ombre=random.uniform(0,1)
                        if grandi_mi_ombre<=0.5:
                            tableau[valeur]+=1
        affichage_arbres(tableau)
        print()











#################################################################################################################
#################################################################################################################
FONCTIONNE TECHNIQUEMENT MAIS YA DES PRBLMS

import random


def affichage_arbres(tableau):
    for i in range(len(tableau)):
        print(tableau[i]*"I")

def croissance_partie_2(tableau, années):
    print(tableau)
    n=(len(tableau)-1)
    for i in range(années):
        for valeur in range(len(tableau)):
            if valeur<n:
                if tableau[valeur-1] > tableau[valeur] and tableau[valeur+1] > tableau[valeur]:
                    tableau[valeur] += 0
                    
                if tableau[valeur-1] < tableau[valeur] and tableau[valeur+1] < tableau[valeur]:
                    grandi_soleil = random.uniform(0,1)
                    if grandi_soleil<=0.9:
                        tableau[valeur]+=random.randint(1,4)
                    else:
                        tableau[valeur]+=random.randint(0,3)
                        
                else:
                    grandi_mi_ombre=random.uniform(0,1)
                    if grandi_mi_ombre<=0.5:
                        tableau[valeur]+=random.randint(1,3)
            else:
                if tableau[valeur-1]<tableau[valeur]:
                    grandi_mi_ombre=random.uniform(0,1)
                    if grandi_mi_ombre<=0.5:
                        tableau[valeur]+=random.randint(1,3)
                else:
                    if tableau[valeur-1]>tableau[valeur]:
                        grandi_mi_ombre=random.uniform(0,1)
                        if grandi_mi_ombre<=0.5:
                            tableau[valeur]+=1
        affichage_arbres(tableau)
        print()




##########################
##################################
##########################################
##################################################
###########################################################
FINAL
J'ai mis bcp de saut de ligne pour se retrouver en travaillant, ca fait grandir le premier et le dernier arbre chaque année, après ca fait grandir a partir du deuxieme jusqu'à l'avant dernier. CHatGPT pour verifier les erreurs d'indentations, compliqué de verif soit meme sachant que avec le teste c'est pas très lisible. Chatgpt a été utilisé pour detecter les doublons

import random


def affichage_arbres(tableau):
    for i in range(len(tableau)):
        print(tableau[i]*"I")
        

def croissance_partie_2(tableau, années):
    print(tableau)
    for i in range(années):
        if tableau[0]<tableau[1]:
            grandi_mi_ombre=random.uniform(0,1)
            if grandi_mi_ombre<=0.5:
                tableau[0]+=random.randint(1,3)
        else :
            grandi_soleil = random.uniform(0,1)
            if grandi_soleil<=0.9:
                tableau[0]+=random.randint(1,4)
        
        if tableau[len(tableau)-1]<tableau[len(tableau)-2]:
            grandi_mi_ombre=random.uniform(0,1)
            if grandi_mi_ombre<=0.5:
                tableau[len(tableau)-1]+=random.randint(1,3)
        else :
            grandi_soleil = random.uniform(0,1)
            if grandi_soleil<=0.9:
                tableau[len(tableau)-1]+=random.randint(1,4)
                    
                    
        for valeur in range(1, len(tableau)-1):
                
            if tableau[valeur-1] > tableau[valeur] and tableau[valeur+1] > tableau[valeur]:
                tableau[valeur] += 0
                    
                    
            elif tableau[valeur-1] < tableau[valeur] and tableau[valeur+1] < tableau[valeur]:
                grandi_soleil = random.uniform(0,1)
                if grandi_soleil<=0.9:
                    tableau[valeur]+=random.randint(1,4)
                else:
                    tableau[valeur]+=random.randint(0,3)
                        
            else:
                grandi_mi_ombre=random.uniform(0,1)
                if grandi_mi_ombre<=0.5:
                    tableau[valeur]+=random.randint(1,3)
        print(tableau) 
       
        affichage_arbres(tableau)
        print() # pour faire une ligne entre chaque années


**********************************************************************************
VERSION DE SAMEDI:

# Créé par nicolasthevenet, le 11/01/2026 en Python 3.7

import random

#PARTIE 2

#ce simulateur prend en compte les voisins de chaque côté d'une valeur, les
#chances de pousser de l'arbre seront réduites si il est encerclé par des arbres
#plus grand
def croissance_partie_2(tableau, années):
    print(tableau)
    for i in range(années):
        if tableau[0]<tableau[1]:
            grandi_mi_ombre=random.uniform(0,1)
            if grandi_mi_ombre<=0.5:
                tableau[0]+=random.randint(1,3)
        else :
            grandi_soleil = random.uniform(0,1)
            if grandi_soleil<=0.9:
                tableau[0]+=random.randint(1,4)

        if tableau[len(tableau)-1]<tableau[len(tableau)-2]:
            grandi_mi_ombre=random.uniform(0,1)
            if grandi_mi_ombre<=0.5:
                tableau[len(tableau)-1]+=random.randint(1,3)
        else :
            grandi_soleil = random.uniform(0,1)
            if grandi_soleil<=0.9:
                tableau[len(tableau)-1]+=random.randint(1,4)


        for valeur in range(1, len(tableau)-1):

            if tableau[valeur-1] > tableau[valeur] and tableau[valeur+1] > tableau[valeur]:
                tableau[valeur] += 0


            elif tableau[valeur-1] < tableau[valeur] and tableau[valeur+1] < tableau[valeur]:
                grandi_soleil = random.uniform(0,1)
                if grandi_soleil<=0.9:
                    tableau[valeur]+=random.randint(1,4)
                else:
                    tableau[valeur]+=random.randint(0,3)

            else:
                grandi_mi_ombre=random.uniform(0,1)
                if grandi_mi_ombre<=0.5:
                    tableau[valeur]+=random.randint(1,3)
        print(tableau)

        affichage_arbres(tableau)


def croissance_partie_2_voisins_eloignes(tableau, annees):
    print(tableau)
    for annee in range(annees):
        for i in range(len(tableau)):
            # vérifier qu'on peut regarder 3 arbres plus loin
            if i + 3 < len(tableau):
                arbres_eloignes = tableau[i+1:i+4]

                # si l'arbre est plus petit que tous les 3 suivants
                if tableau[i]>minimum(arbres_eloignes):
                    for i in range (arbres_eloignes):
                        tableau[i]+=random.randint(1,2)
                else:
                    tableau[i]+=random.randint(1,3)
        affichage_arbres(tableau)
        print()

#version chat gpt pour s'aider
def croissance_voisins_eloignes(tableau, annees):

    for annee in range(annees):

        # on parcourt tous les arbres
        for i in range(len(tableau)):

            # vérifier qu'il y a bien 3 arbres après
            if i + 3 < len(tableau):

                # arbre dominant
                arbre_central = tableau[i]
                arbres_suivants = tableau[i+1:i+4]

                # si l'arbre est plus grand que les 3 suivants
                if arbre_central > max(arbres_suivants):

                    # les 3 arbres suivants poussent moins
                    for j in range(i+1, i+4):
                        tableau[j] += random.randint(0, 1)

                else:
                    # croissance normale des 3 arbres
                    for j in range(i+1, i+4):
                        tableau[j] += random.randint(1, 3)

        affichage_arbres(tableau)
        print()







